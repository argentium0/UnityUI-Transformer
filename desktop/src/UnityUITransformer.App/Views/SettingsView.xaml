<UserControl x:Class="UnityUITransformer.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <Border Background="#1A1B1E" BorderBrush="#333333" BorderThickness="1" CornerRadius="16" Padding="24" Width="480" HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header Title & Close -->
                <RowDefinition Height="Auto"/> <!-- Subtitle Divider -->
                <RowDefinition Height="Auto"/> <!-- Option 1: Disconnect Account -->
                <RowDefinition Height="Auto"/> <!-- Option 2: Download User Manual -->
                <RowDefinition Height="Auto"/> <!-- Status / Feedback Message -->
            </Grid.RowDefinitions>

            <!-- Header Title Bar -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="⚙" Foreground="#D4FF32" FontSize="18" Margin="0,0,8,0" VerticalAlignment="Center"/>
                    <TextBlock Text="Settings &amp; Preferences" Foreground="#FFFFFF" FontSize="16" FontWeight="Bold" VerticalAlignment="Center"/>
                </StackPanel>
                <Button Command="{Binding CloseSettingsCommand}" HorizontalAlignment="Right" Width="28" Height="28" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Close Settings">
                    <TextBlock Text="✕" Foreground="#A0A0A5" FontSize="14" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}" CornerRadius="14">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2A2B30"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>

            <!-- Subtitle Divider -->
            <Border Grid.Row="1" Height="1" Background="#26272B" Margin="0,0,0,20"/>

            <!-- Option 1: Disconnect Account -->
            <Border Grid.Row="2" Background="#141517" BorderBrush="#26272B" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="Figma Connection" Foreground="#FFFFFF" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                        <TextBlock Text="Clear local session cache, purge DPAPI tokens, and return to login." Foreground="#9E9E9E" FontSize="11.5" TextWrapping="Wrap"/>
                    </StackPanel>
                    <Button Grid.Column="1" Command="{Binding DisconnectCommand}" Margin="12,0,0,0" Padding="14,8" Background="#2A1719" BorderBrush="#EF4444" BorderThickness="1" Foreground="#F87171" FontWeight="SemiBold" FontSize="12" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Disconnect Account"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Option 2: Download User Manual -->
            <Border Grid.Row="3" Background="#141517" BorderBrush="#26272B" BorderThickness="1" CornerRadius="12" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="User Manual &amp; Setup Guide" Foreground="#FFFFFF" FontWeight="SemiBold" FontSize="13" Margin="0,0,0,4"/>
                        <TextBlock Text="Export complete documentation (UserManual.md) to your local disk." Foreground="#9E9E9E" FontSize="11.5" TextWrapping="Wrap"/>
                    </StackPanel>
                    <Button Grid.Column="1" Command="{Binding DownloadManualCommand}" Margin="12,0,0,0" Padding="14,8" Background="#1B261A" BorderBrush="#10B981" BorderThickness="1" Foreground="#D4FF32" FontWeight="SemiBold" FontSize="12" Cursor="Hand">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="📄 Download Manual"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>

            <!-- Status / Feedback Message -->
            <Border Grid.Row="4" Visibility="{Binding HasSettingsStatusMessage, Converter={StaticResource BoolToVisConverter}}" Background="#192A20" BorderBrush="#10B981" BorderThickness="1" CornerRadius="8" Padding="12,8" Margin="0,4,0,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="✓ " Foreground="#10B981" FontWeight="Bold" FontSize="12"/>
                    <TextBlock Text="{Binding SettingsStatusMessage}" Foreground="#FFFFFF" FontSize="12" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
