<Application x:Class="UnityUITransformer.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:UnityUITransformer.App"
             xmlns:converters="clr-namespace:UnityUITransformer.App.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <!-- Converters -->
            <converters:BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBoolConverter"/>
            <converters:StringNullOrEmptyToVisibilityConverter x:Key="NullOrEmptyToVisConverter"/>
            <converters:StepActiveToOpacityConverter x:Key="StepOpacityConverter"/>
            <converters:StepActiveToBorderBrushConverter x:Key="StepBorderBrushConverter"/>
            <converters:LogLevelToBrushConverter x:Key="LogLevelToBrushConverter"/>
            <converters:EqualityToBooleanConverter x:Key="EqualityToBoolConverter"/>

            <!-- True Dribbble Dark Dashboard Palette -->
            <Color x:Key="ColorBgDeep">#18181A</Color>          <!-- Deep Matte Black Canvas -->
            <Color x:Key="ColorBgCard">#25262B</Color>          <!-- Elevated Charcoal Card Panel -->
            <Color x:Key="ColorBgHeader">#18181A</Color>        <!-- Deep Black Header -->
            <Color x:Key="ColorBgSidebar">#121214</Color>       <!-- Dark Vertical Left Sidebar -->
            <Color x:Key="ColorBgInput">#1F2024</Color>         <!-- Inset Deep Charcoal Input -->
            <Color x:Key="ColorTerminal">#141416</Color>        <!-- Pitch Charcoal Terminal -->

            <Color x:Key="ColorBorderSubtle">#2D2E34</Color>
            <Color x:Key="ColorBorderFocus">#D4FF32</Color>     <!-- Neon Lime Green Focus Ring -->

            <!-- Accents -->
            <Color x:Key="ColorAccentNeon">#D4FF32</Color>      <!-- Neon Lime Green -->
            <Color x:Key="ColorAccentBlue">#4A62FF</Color>      <!-- Royal Blue Primary CTA -->
            <Color x:Key="ColorAccentBlueHover">#3B52E6</Color>
            <Color x:Key="ColorSuccessEmerald">#10B981</Color>
            <Color x:Key="ColorWarningAmber">#F59E0B</Color>
            <Color x:Key="ColorErrorRed">#EF4444</Color>

            <Color x:Key="ColorTextPrimary">#FFFFFF</Color>     <!-- Pure White Headers -->
            <Color x:Key="ColorTextSecondary">#9E9E9E</Color>   <!-- Muted Slate Subtext -->
            <Color x:Key="ColorTextMuted">#6E6E6E</Color>

            <!-- Brushes -->
            <SolidColorBrush x:Key="BrushBgDeep" Color="{StaticResource ColorBgDeep}"/>
            <SolidColorBrush x:Key="BrushBgCard" Color="{StaticResource ColorBgCard}"/>
            <SolidColorBrush x:Key="BrushBgHeader" Color="{StaticResource ColorBgHeader}"/>
            <SolidColorBrush x:Key="BrushBgSidebar" Color="{StaticResource ColorBgSidebar}"/>
            <SolidColorBrush x:Key="BrushBgInput" Color="{StaticResource ColorBgInput}"/>
            <SolidColorBrush x:Key="BrushTerminal" Color="{StaticResource ColorTerminal}"/>

            <SolidColorBrush x:Key="BrushBorderSubtle" Color="{StaticResource ColorBorderSubtle}"/>
            <SolidColorBrush x:Key="BrushBorderFocus" Color="{StaticResource ColorBorderFocus}"/>

            <SolidColorBrush x:Key="BrushAccentNeon" Color="{StaticResource ColorAccentNeon}"/>
            <SolidColorBrush x:Key="BrushAccentBlue" Color="{StaticResource ColorAccentBlue}"/>
            <SolidColorBrush x:Key="BrushAccentCyan" Color="{StaticResource ColorAccentNeon}"/>
            <SolidColorBrush x:Key="BrushSuccessEmerald" Color="{StaticResource ColorSuccessEmerald}"/>
            <SolidColorBrush x:Key="BrushWarningAmber" Color="{StaticResource ColorWarningAmber}"/>
            <SolidColorBrush x:Key="BrushErrorCoral" Color="{StaticResource ColorErrorRed}"/>

            <SolidColorBrush x:Key="BrushTextPrimary" Color="{StaticResource ColorTextPrimary}"/>
            <SolidColorBrush x:Key="BrushTextSecondary" Color="{StaticResource ColorTextSecondary}"/>
            <SolidColorBrush x:Key="BrushTextMuted" Color="{StaticResource ColorTextMuted}"/>

            <!-- Royal Blue Pill-Shaped Primary Button Style -->
            <Style x:Key="ProMaxPrimaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="{StaticResource BrushAccentBlue}"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="FontFamily" Value="Segoe UI Variable, Inter, sans-serif"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="13.5"/>
                <Setter Property="Padding" Value="26,12"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                     Background="{TemplateBinding Background}" 
                                     CornerRadius="24" 
                                     Padding="{TemplateBinding Padding}"
                                     SnapsToDevicePixels="True">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#3B52E6"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1.03" ScaleY="1.03"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.98" ScaleY="0.98"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Background" Value="#2A2B32"/>
                                    <Setter Property="Foreground" Value="#6E6E6E"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Deep Charcoal Pill Secondary Button Style -->
            <Style x:Key="ProMaxSecondaryButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="#1F2024"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontFamily" Value="Segoe UI Variable, Inter, sans-serif"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Padding" Value="18,9"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" 
                                     Background="{TemplateBinding Background}" 
                                     CornerRadius="20" 
                                     Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2B2C32"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18181A"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.4"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Deep Charcoal TextBox Style with 1px Neon Lime Focus Border -->
            <Style x:Key="ProMaxTextBoxStyle" TargetType="TextBox">
                <Setter Property="Background" Value="#1F2024"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="14,11"/>
                <Setter Property="FontSize" Value="12.5"/>
                <Setter Property="FontFamily" Value="Cascadia Code, Consolas, monospace"/>
                <Setter Property="CaretBrush" Value="#D4FF32"/>
                <Setter Property="SelectionBrush" Value="#4A62FF"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TextBox">
                            <Border x:Name="border" 
                                     Background="{TemplateBinding Background}" 
                                     BorderBrush="Transparent" 
                                     BorderThickness="1" 
                                     CornerRadius="12">
                                <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsFocused" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#D4FF32"/>
                                    <Setter TargetName="border" Property="BorderThickness" Value="1"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#25262C"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Sleek High-Contrast Circular Step Button Style (48x48 Circle) -->
            <Style x:Key="SidebarCircleButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="48"/>
                <Setter Property="Height" Value="48"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="FontFamily" Value="Segoe UI Variable, Inter, sans-serif"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
                <Setter Property="RenderTransform">
                    <Setter.Value>
                        <ScaleTransform ScaleX="1.0" ScaleY="1.0"/>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="#2A2B30"
                                    CornerRadius="24"
                                    SnapsToDevicePixels="True">
                                <ContentPresenter TextElement.Foreground="{TemplateBinding Foreground}" 
                                                  HorizontalAlignment="Center" 
                                                  VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#3F4045"/>
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="1.08" ScaleY="1.08"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="RenderTransform">
                                        <Setter.Value>
                                            <ScaleTransform ScaleX="0.95" ScaleY="0.95"/>
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="border" Property="Opacity" Value="0.35"/>
                                    <Setter Property="Cursor" Value="No"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Small Info 'i' ToggleButton Style -->
            <Style x:Key="InfoToggleButtonStyle" TargetType="ToggleButton">
                <Setter Property="Background" Value="#1F2024"/>
                <Setter Property="Foreground" Value="#9E9E9E"/>
                <Setter Property="BorderBrush" Value="#3F3F46"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToggleButton">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="9"
                                    SnapsToDevicePixels="True">
                                <TextBlock Text="i" FontFamily="Georgia, serif" FontStyle="Italic" FontWeight="Bold" FontSize="11" Foreground="{TemplateBinding Foreground}" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-1,0,0"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="BorderBrush" Value="#D4FF32"/>
                                    <Setter Property="Foreground" Value="#D4FF32"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18181A"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#D4FF32"/>
                                    <Setter Property="Foreground" Value="#D4FF32"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Dark Theme CheckBox Style -->
            <Style x:Key="ProMaxCheckBoxStyle" TargetType="CheckBox">
                <Setter Property="Foreground" Value="#9E9E9E"/>
                <Setter Property="FontSize" Value="11.5"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                <Border x:Name="border" Width="14" Height="14" CornerRadius="3" Background="#1F2024" BorderBrush="#3F3F46" BorderThickness="1" Margin="0,0,6,0">
                                    <TextBlock x:Name="checkMark" Text="✓" Foreground="#D4FF32" FontWeight="Bold" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="Collapsed"/>
                                </Border>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#18181A"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#D4FF32"/>
                                    <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Dark ToolTip Style -->
            <Style TargetType="ToolTip">
                <Setter Property="Background" Value="#25262B"/>
                <Setter Property="Foreground" Value="#FFFFFF"/>
                <Setter Property="BorderBrush" Value="#3F3F46"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10,6"/>
                <Setter Property="FontSize" Value="11.5"/>
                <Setter Property="FontFamily" Value="Segoe UI Variable, Inter, sans-serif"/>
                <Setter Property="HasDropShadow" Value="False"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ToolTip">
                            <Border Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter />
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Custom ScrollBar Styling for Terminal -->
            <Style TargetType="ScrollBar">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Width" Value="8"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ScrollBar">
                            <Grid x:Name="Bg" SnapsToDevicePixels="true">
                                <Track x:Name="PART_Track" IsDirectionReversed="true" IsEnabled="{TemplateBinding IsMouseOver}">
                                    <Track.Thumb>
                                        <Thumb x:Name="thumb">
                                            <Thumb.Template>
                                                <ControlTemplate TargetType="Thumb">
                                                    <Border Background="#3F3F46" CornerRadius="4"/>
                                                </ControlTemplate>
                                            </Thumb.Template>
                                        </Thumb>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
