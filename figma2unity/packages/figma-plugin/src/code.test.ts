import { describe, it, expect } from 'vitest';

describe('Figma Plugin Scaffold', () => {
  it('hello world test passes', () => {
    const pluginName = 'Figma2Unity';
    expect(pluginName).toBe('Figma2Unity');
  });
});
